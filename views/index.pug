extends layout

block content
  if (currentUser === undefined) 
    h1 Welcome to the Messageboard! 
    p You must sign in to make a post.
  else 
    h1 Welcome !{currentUser.name.first_name}, !{currentUser.name.last_name} (!{currentUser.username})! 
    

  
  ul All mesages
    li !{allMessages}
