extends layout

block content

  h1 Become a premium member! 
  h2 This will allow you to view the authors of posts

  form(method='POST')
    div.form-group
      label(for='secretCode') Secret Code:
      input#name.form-control(type='password', placeholder='What bike is Marquez on?' name='secretCode' required)
    button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg

